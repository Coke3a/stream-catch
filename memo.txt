Vercel
    - Next.js 14 + React Server Components + Tailwind
    - Deploy: Vercel ฟรี tier
    
Fly.io 
    - สำหรับ main backend rust app เพื่อเชื่อมต่อกับ frontend และจะสร้าง job เท่านั้น
    - รับ webhook จาก Stripe

DigitalOcean (VPS: 2 vCPU, 2GB RAM, 50–80GB disk (ค่อย scaling to VPS: 2 vCPU, 4GB RAM, 80–100GB disk ในอนาคต))
    - Rust worker + rust-headless-chrome rust app สำหรับ do job
    - OneLiveRec สำหรับ recording

Payments:
  - Stripe (Thailand) → PromptPay QR + Card
  
Video Storage & Delivery:
  - Backblaze B2 (private bucket + SSE-C AES256)
  - Cloudflare Free Plan (cache + Workers สำหรับ signed URL + entitlement check)
  - Lifecycle Rule: Delete after 30 days → storage ไม่เกิน 1 TB
  
Database:
  - PostgreSQL via Supabase (แนะนำตัวนี้มากกว่า Neon)
    ├── ฟรี tier: 500 MB storage, 5M row reads, connection pooling ในตัว
    └── pg-vector, auth, storage (ถ้าจะเก็บ poster.jpg ชั่วคราว)

